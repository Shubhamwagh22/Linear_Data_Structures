def rotation (str1,str2):
  if len(str1) != len(str2) or len(str1) == 0:
    return False

  str3 = str1 + str2

  if str2 in str3:
    return True
  else:
    return False

str1 = "edyoda"
str2 = "dayode"
print(rotation(str1,str2))

###################################
Output:
True
